import { RootSiblingParent } from "react-native-root-siblings";

export default function App() {
  return (
    <RootSiblingParent>
      {
        // Resto da aplicação: Contextos, rota, etc
      }
    </RootSiblingParent>
  );
}
